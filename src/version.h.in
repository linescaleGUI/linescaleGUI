/**
 * @file version.h
 * @brief Autogenerated version file by cmake.
 */

#pragma once
#ifndef _version_h_
#define _version_h_

namespace version {
/**
 * @brief The URL of the remote git repository.
 */
constexpr char repoUrl[] = "${GIT_REPO_URL}";
/**
 * @brief The current version in `<major>.<minor>.<patch>`.
 */
constexpr char version[] = "${PROJECT_VERSION}";
/**
 * @brief The current git revision hash.
 */
constexpr char gitRev[] = "${GIT_REV}";
/**
 * @brief The build date in ´YYYY-MM-dd´.
 */
constexpr char buildDate[] = "${BUILD_DATE}";
/**
 * @brief The build time in ´HH:MM:SS´.
 */
constexpr char buildTime[] = "${BUILD_TIME}";
/**
 * @brief Whether the source repository contains changes at the time of the build.
 */
constexpr bool repoDirty = ${GIT_REPO_DIRTY};
}  // namespace version

#endif  //_version_h_
